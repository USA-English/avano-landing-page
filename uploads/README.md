# Uploads

Put production logo/image assets in this folder.

The particle field currently expects the exact AVANO logo SVG at:

```text
uploads/avano-logo.svg
```

Use that exact filename so the animation samples the real logo instead of any generated fallback.
