# Typography Fonts

Upload project typography files here.

Recommended formats:
- `.woff2` for production web fonts
- `.woff` as fallback when needed
- source files such as `.otf` or `.ttf` only when required
